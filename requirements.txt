# Core
pyyaml>=6.0
requests>=2.28

# Metrics & plotting
matplotlib>=3.7
numpy>=1.24

# Testing
pytest>=7.4
pytest-cov>=4.1

# Optional: GPU monitoring (only needed for DGX dashboard)
# pynvml>=11.5
# rich>=13.0

# Optional: DGX real deployment
# vllm>=0.4.0  (install separately on DGX — GPU build required)
